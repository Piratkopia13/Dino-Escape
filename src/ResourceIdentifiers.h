#pragma once

// Resource identifiers specified below

namespace Fonts {
	enum ID {
		Main
	};
}

namespace Textures {
	enum ID {
		Dino,
		Enemies,
		Items,
		HealthBar,
		MenuBackground,
		ControlsBackground,
		Default
	};
}

namespace Music {
	enum ID {
		MenuTheme,
		GameTheme
	};
}

namespace Sounds {
	enum ID {
		Jump,
		Shoot,
		ShootFireball,
		HitEnemy,
		HitPlayer,
		Select,
		Pickup,
		WinJingle,
		LoseJingle
	};
}

namespace PostEffects {
	enum ID {
		Vignette
	};
}