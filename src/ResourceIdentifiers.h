#pragma once


namespace Fonts {
	enum ID {
		Main,
		Roboto
	};
}

namespace Textures {
	enum ID {
		DINO,
		ENEMIES,
		ITEMS,
		HEALTH_BAR,
		MENU_BACKGROUND
	};
}

namespace Music {
	enum ID {
		MenuTheme,
		GameTheme
	};
}

namespace Sounds {
	enum ID {
		Jump,
		Shoot,
		ShootFireball,
		HitEnemy,
		HitPlayer,
		Select,
		Pickup,
		WinJingle,
		LoseJingle
	};
}